# 챌린지별 예상 수정 분량

이 문서는 `challenges/*/bug.c`의 코드와 각 문제의 `TODO`를 기준으로, 정상적인
해결에 필요한 **C 코드 변경 줄 수**를 대략 추정한 것이다.

## 산정 기준

- 숫자는 추가·삭제·변경되는 **논리적 코드 줄**의 합이다.
- 주석, 디버깅 로그, 별도 테스트 코드는 포함하지 않는다.
- 단순히 크래시만 피하는 임시 처리가 아니라, 문제에서 요구한 메모리 수명·경계·소유권
  규칙을 회복하는 수정을 기준으로 한다.
- 중괄호 배치와 오류 처리 방식에 따라 실제 diff는 몇 줄 정도 달라질 수 있다.

## 문제별 예상치

| # | 챌린지 | 예상 변경량 | 분량 | 주된 수정 내용 |
|---:|---|---:|---|---|
| 01 | `use_after_free` | 9~14줄 | 큼 | 이벤트에서는 `closed`만 표시하고, `Screen`이 닫힌 위젯을 해제한 뒤 슬롯을 `NULL`로 만들며 순회 시 빈 슬롯을 건너뛴다. |
| 02 | `stack_buffer_overflow` | 1줄 | 매우 짧음 | 행 루프의 `i <= rows`를 `i < rows`로 고친다. |
| 03 | `heap_buffer_overflow` | 1줄 | 매우 짧음 | `realloc` 크기에 이전 `cap`이 아니라 `newcap`을 사용한다. |
| 04 | `double_free` | 3~5줄 | 짧음 | 비소유 인덱스인 `by_name`을 해제하는 두 번째 루프를 제거한다. |
| 05 | `null_return_deref` | 1~3줄 | 짧음 | `cfg_get()` 결과가 `NULL`인지 검사하고 기본값 또는 오류로 처리한다. |
| 06 | `null_deref` | 1~2줄 | 매우 짧음 | `strchr()`가 `NULL`을 반환하면 해당 줄을 건너뛴다. |
| 07 | `stack_use_after_return` | 5~9줄 | 보통 | `LineView`가 포인터 배열을 직접 소유하게 하고 `split_lines()`가 그 저장소에 결과를 채우도록 바꾼다. |
| 08 | `uninitialized_read` | 2~4줄 | 짧음 | 행 포인터 표를 0으로 초기화하고 `NULL` 행을 읽지 않도록 한다. |
| 09 | `strcpy_overflow` | 1줄 | 매우 짧음 | 크기 계산 루프가 마지막 조각까지 포함하도록 `i < n`으로 고친다. |
| 10 | `realloc_dangling` | 4~7줄 | 보통 | undo 스냅샷에 원본 주소가 아닌 `malloc`과 `memcpy`로 만든 독립 복사본을 저장한다. |
| 11 | `global_overflow` | 5~9줄 | 보통 | 아레나의 남은 크기를 검사하고 부족할 때 `NULL`을 반환하며 호출자가 이를 처리한다. |
| 12 | `free_non_heap` | 3~5줄 | 짧음 | 내부 필드 포인터는 해제하지 않고 `strdup()`이 반환한 `base`만 한 번 해제한다. |
| 13 | `linked_list_uaf` | 3~5줄 | 짧음 | 현재 노드를 해제하기 전에 `next`를 지역 변수에 저장하고 그 값으로 이동한다. |
| 14 | `integer_overflow_alloc` | 8~14줄 | 큼 | 바이트 수를 `size_t`로 바꾸고 각 곱셈 전에 `SIZE_MAX` 기반 오버플로 검사를 추가한다. |
| 15 | `dangling_in_struct` | 2~4줄 | 짧음 | 로그아웃 시 `Session.user`를 `NULL`로 만들고 요청 처리 전에 검사한다. |
| 16 | `unused_cap_overflow` | 4~8줄 | 보통 | 구분자·필드·종료 문자를 포함한 필요 크기를 계산하고 `cap` 초과 시 잘라 담거나 오류 처리한다. |
| 17 | `ownership_uaf` | 1~5줄 | 짧음 | 메시지 소유자를 한 곳으로 정하고, 비소유 `log` 또는 소비자가 같은 메시지를 다시 해제하지 않게 한다. |
| 18 | `cleanup_double_free` | 2줄 | 매우 짧음 | 조기 `free(c->tx)`를 없애고 모든 자원이 정리되는 올바른 cleanup 라벨로 이동한다. |
| 19 | `realloc_shrink_overflow` | 1~4줄 | 짧음 | 축소 성공 시 `cap`과 함께 `len`도 `keep`으로 갱신한다. |
| 20 | `vector_stale_pointer` | 4~6줄 | 보통 | 성장 전 원소 주소 대신 인덱스를 저장하고 이후 `h.data[index]`로 다시 접근한다. |

## 요약

| 분량 | 기준 | 문제 수 | 해당 문제 |
|---|---:|---:|---|
| 매우 짧음 | 1~2줄 | 5개 | 02, 03, 06, 09, 18 |
| 짧음 | 대체로 3~5줄 이내 | 8개 | 04, 05, 08, 12, 13, 15, 17, 19 |
| 보통 | 대체로 4~9줄 | 5개 | 07, 10, 11, 16, 20 |
| 큼 | 8줄 이상 또는 여러 함수 수정 | 2개 | 01, 14 |

전체 20문제의 예상 변경량은 약 **61~109줄**이다. 따라서 대부분은 수정 코드 자체가
짧고, 실제 학습 난이도는 수정 분량보다 **원인 추적과 올바른 소유권 판단**에서 나온다.

> 참고: 14번은 안전한 크기 계산을 고친 뒤에도 요청 자체가 약 16 GiB이므로, 실제
> 프로그램에서는 최대 이미지 크기 제한이나 할당 실패 처리까지 별도로 고려하는 편이 좋다.
