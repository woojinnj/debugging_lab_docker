# 메모리 디버깅 챌린지 난이도 순위

이 순위는 **쉬운 문제 → 어려운 문제** 순서다. 단순히 수정 코드의 길이만 보지 않고,
다음 요소를 함께 고려했다.

- 크래시가 발생한 위치와 실제 원인이 얼마나 떨어져 있는가
- 포인터의 수명과 메모리 소유권을 얼마나 많이 추적해야 하는가
- GDB에서 원인을 확인하기 위해 필요한 중단점과 상태 비교의 수
- `realloc`, 함수 포인터, 정수 오버플로처럼 추가 개념이 필요한가
- 증상만 막는 것이 아니라 올바른 구조로 수정하기가 얼마나 어려운가

> 난이도는 학습용 권장 순서이며 절대적인 기준은 아니다. 익숙한 개념에 따라 체감
> 난이도는 달라질 수 있다.

## 전체 순위

| 순위 | 챌린지 | 단계 | 핵심 개념 | 난이도 판단 이유 |
|---:|---|---|---|---|
| 1 | `06_null_deref` | 입문 | NULL 포인터 역참조 | 크래시 줄에서 포인터 값을 확인하면 원인이 바로 드러난다. |
| 2 | `05_null_return_deref` | 입문 | 함수의 NULL 반환값 | libc 안에서 죽지만 호출 프레임 하나만 올라가면 원인을 확인할 수 있다. |
| 3 | `16_unused_cap_overflow` | 입문 | 용량 검사 없는 쓰기 | `len`, `cap`, 추가 길이만 비교하면 경계 초과를 쉽게 입증할 수 있다. |
| 4 | `02_stack_buffer_overflow` | 입문 | off-by-one, 스택 카나리 | 크래시 위치는 늦게 나타나지만 루프 경계와 배열 크기를 비교하면 명확하다. |
| 5 | `12_free_non_heap` | 초급 | 내부 포인터, 올바른 `free` 대상 | 각 필드 주소를 원본 버퍼 시작 주소와 비교하면 소유 관계가 보인다. |
| 6 | `18_cleanup_double_free` | 초급 | 오류 경로, 이중 해제 | `goto` 경로를 따라 같은 주소가 두 번 해제되는지만 추적하면 된다. |
| 7 | `09_strcpy_overflow` | 초급 | 할당 크기와 복사량 불일치 | 계산 루프와 복사 루프의 범위를 나란히 비교해야 한다. |
| 8 | `11_global_overflow` | 초급 | 전역 아레나 경계 검사 | 크래시는 `memcpy`에서 나지만 offset과 전체 용량을 비교하면 원인을 찾을 수 있다. |
| 9 | `04_double_free` | 중급 | 별칭과 소유권 | 서로 다른 두 배열이 같은 객체를 가리킨다는 사실을 이해해야 한다. |
| 10 | `19_realloc_shrink_overflow` | 중급 | `len`/`cap` 불변식 | 버퍼 축소 후 논리 길이와 실제 용량이 어긋났음을 찾아야 한다. |
| 11 | `03_heap_buffer_overflow` | 중급 | 동적 배열 성장 불변식 | 구조체의 논리적 `cap`과 실제 할당 크기를 별도로 추적해야 한다. |
| 12 | `08_uninitialized_read` | 중급 | 미초기화 포인터, 힙 재사용 | 쓰레기 값이 NULL처럼 보장되지 않는다는 점과 할당기 재사용을 이해해야 한다. |
| 13 | `13_linked_list_uaf` | 중급 | 연결 리스트 UAF | 노드 해제 이후 `next`를 읽는 순서 문제를 반복문 흐름 속에서 찾아야 한다. |
| 14 | `15_dangling_in_struct` | 중상급 | 구조체 내부 댕글링 포인터 | 해제, 힙 재사용, 함수 포인터 호출이 서로 다른 단계에 걸쳐 있다. |
| 15 | `07_stack_use_after_return` | 중상급 | 스택 객체 수명 | 주소 자체는 그럴듯하지만 함수 반환 후 내용이 무효라는 수명 개념이 필요하다. |
| 16 | `14_integer_overflow_alloc` | 중상급 | 정수 승격, 과소 할당 | 크래시가 쓰기 루프에서 나며 그보다 앞선 정수 곱셈의 타입까지 분석해야 한다. |
| 17 | `17_ownership_uaf` | 고급 | 공유 참조와 소유권 이전 | 발행, 배달, 감사 로그, 종료 코드에 흩어진 해제 책임을 전체적으로 추적해야 한다. |
| 18 | `10_realloc_dangling` | 고급 | `realloc`과 스냅샷 소유권 | 과거 주소가 언제 무효가 됐는지와 스냅샷이 복사인지 별칭인지 구분해야 한다. |
| 19 | `20_vector_stale_pointer` | 고급 | 배열 성장 후 원소 포인터 무효화 | 포인터가 처음에는 유효하고 나중의 `realloc` 이후에만 무효가 되는 시간적 추적이 필요하다. |
| 20 | `01_use_after_free` | 고급 | UAF, 힙 재사용, 함수 포인터 | 해제 지점과 크래시 지점이 멀고, 재사용된 객체의 손상된 vtable까지 역추적해야 한다. |

## 권장 학습 구간

### 1단계: 크래시 지점 읽기

`06 → 05 → 16 → 02`

`run`, `bt`, `frame`, `list`, `print`, `info locals` 사용에 익숙해지는 구간이다.

### 2단계: 경계와 해제 규칙

`12 → 18 → 09 → 11`

할당 시작 주소, 버퍼 크기, 실제 접근 범위, 동일 주소의 해제 횟수를 비교한다.

### 3단계: 자료구조 불변식

`04 → 19 → 03 → 08 → 13`

`len <= cap`, 소유 포인터는 하나, 해제 후 접근 금지 같은 불변식을 중심으로 본다.

### 4단계: 객체 수명과 타입

`15 → 07 → 14`

힙과 스택의 수명 차이, 함수 반환 후 주소의 유효성, 크기 계산의 정수 타입을 다룬다.

### 5단계: 복합 소유권과 시간에 따른 포인터 변화

`17 → 10 → 20 → 01`

포인터가 생성될 때는 유효했지만 해제나 `realloc` 뒤에 무효가 되는 과정을 여러
중단점에서 비교한다.

## 문제 하나를 푸는 기준

각 문제에서 다음 다섯 가지를 설명할 수 있으면 완료한 것으로 본다.

1. 어떤 신호(`SIGSEGV`, `SIGABRT`, `SIGBUS`)로 종료됐는가?
2. GDB가 보여준 직접적인 크래시 위치는 어디인가?
3. 잘못된 포인터·인덱스·크기는 어디에서 만들어졌는가?
4. 수정이 메모리 수명·경계·소유권 규칙을 어떻게 회복하는가?
5. 수정 후 프로그램이 종료 코드 `0`으로 끝나는가?

문제별 기본 실행 명령은 다음과 같다.

```bash
make run NAME=06_null_deref
make gdb NAME=06_null_deref
```

수정 후에는 다시 실행하고 종료 코드를 확인한다.

```bash
make run NAME=06_null_deref
./build/06_null_deref
echo $?
```
